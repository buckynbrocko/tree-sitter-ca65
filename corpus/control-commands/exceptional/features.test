================================================================================
Features Control-Commands
================================================================================

.autoimport +
.autoimport -
.case +
.case -
.linecont +
.linecont -

--------------------------------------------------------------------------------

(source_file
  (single_feature_command
    (command)
    quantifier: (plus))
  (single_feature_command
    (command)
    quantifier: (minus))
  (single_feature_command
    (command)
    quantifier: (plus))
  (single_feature_command
    (command)
    quantifier: (minus))
  (single_feature_command
    (command)
    quantifier: (plus))
  (single_feature_command
    (command)
    quantifier: (minus)))
